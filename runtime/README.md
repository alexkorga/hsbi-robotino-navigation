# Runtime Artifacts

Generated calibration CSV files, phase checkpoints, and TensorBoard logs are
written here and ignored by version control. Camera recordings go to the
separately ignored `recordings/camera/` directory; trained models and their
evaluation reports go under `models/`. Nothing in `runtime/` is an authoritative
input to the navigation runtime.
